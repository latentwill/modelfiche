[project]
name = "titles-dam-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
titles-worker = "titles_worker.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["titles_worker"]
